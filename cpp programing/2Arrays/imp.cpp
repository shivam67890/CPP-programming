/* 1 >> In sets we ddont use set[i] insteadwe use sets.find(i) == sets.end()

revise xor method in array⭐⭐⭐


unordered_map<int, int> hash; ✅ is better then int hash[n + 1] = {0};❌( it has limitation.. )



mpp.find(two) != mpp.end() is used to find wheather two is present in mpp or not ..



next_permutation(arr.begin(),arr.end());   for finding next larger permutation num 
ex=123==132